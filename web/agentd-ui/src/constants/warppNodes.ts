export const WARPP_STEP_NODE_DIMENSIONS = {
  defaultWidth: 320,
  defaultHeight: 260,
  minWidth: 320,
  minHeight: 260,
} as const

// Visual/interactive footprint when a step node is collapsed
export const WARPP_STEP_NODE_COLLAPSED = {
  width: 160,
  height: 72,
} as const

export const WARPP_UTILITY_NODE_DIMENSIONS = {
  defaultWidth: 320,
  defaultHeight: 200,
  minWidth: 320,
  minHeight: 200,
} as const

// Visual/interactive footprint when a utility node is collapsed
export const WARPP_UTILITY_NODE_COLLAPSED = {
  width: 220,
  height: 72,
} as const

export const WARPP_GROUP_NODE_DIMENSIONS = {
  defaultWidth: 400,
  defaultHeight: 320,
  minWidth: 280,
  minHeight: 200,
} as const
