{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Ensure noEmit is explicitly set here so allowImportingTsExtensions from base config is valid
    "noEmit": true,
    "types": ["node", "vitest/globals"]
  },
  "include": ["env.d.ts", "src/**/*", "tests/**/*.ts"],
  "exclude": ["e2e"]
}
