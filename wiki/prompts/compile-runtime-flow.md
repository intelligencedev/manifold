# Prompt: Compile Runtime Flow

For a runtime flow, include:

- trigger/entrypoint;
- actors/components;
- ordered `sequenceDiagram` or lifecycle `stateDiagram-v2`;
- persistence/tool/network side effects;
- errors, retries, timeouts, and cancellation behavior;
- tests and evidence.
