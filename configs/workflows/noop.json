{
  "intent": "noop",
  "description": "Return a greeting.",
  "keywords": ["noop"],
  "steps": [
    {
      "id": "s1",
      "text": "Invoke an LLM",
      "tool": {
        "name": "llm_transform",
        "args": {
          "instruction": "You are a helpful assistant.",
          "input": "${A.query}"
        }
      },
      "publish_result": true
    }
  ]
}
